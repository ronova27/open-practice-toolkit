# Codex Prompt: Web/PWA Shell

You are working on `workstream/web-pwa-shell` in the `open-practice-toolkit` repository.

Focus only on frontend app shell planning, offline-first/PWA architecture, local storage boundaries, accessibility baseline, navigation structure, and beginner-friendly interaction patterns. Avoid touching unrelated modules. Produce a small reviewable patch set with tests or verification notes when applicable. Do not add secrets, paid services, deployments, or form submissions.

Goal: propose the first usable web shell without adding a backend or paid infrastructure.
